# Dedicated port so this can run alongside demo-virtual-threads (8080) and the dashboard (8081).
server.port=8082
spring.application.name=demo-reactive-benchmark

# Keep Netty's event loop small on purpose: reactive and virtual threads should scale
# far past this without one carrier thread per in-flight request.
reactor.netty.ioWorkerCount=4
